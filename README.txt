Hello! You should come in PHPMyAdmin and import user_kg.sql, then create virtual hosting on Apache to open index.php. Open file config.php and change the parameters for your settings.

Description:

All users have a role: they can be admins or users. Admins can view every user, create new users, delete any users and update data of exist users. User can only view info about every user.